# TDD Implementation Plan: Provider-First AT Protocol Registration

## Inputs
- Requirements: `01-requirements.md`
- Tests: `02-acceptance-tests.md`
- Document review: `03-document-review.md`
- Coding plan: `04-coding-plan.md`

## Implementation Rules
- Do not implement behavior without a linked requirement ID.
- Write or update one failing test before its minimum implementation.
- Run the smallest relevant test first.
- Refactor only after tests pass.
- Keep traceability and execution evidence updated after every test loop.
- Preserve handle-first login, deletion reauthentication, credential boundaries, and lifecycle fences.

## Test Order
| Step | Test IDs | Requirement IDs | Acceptance Criteria | Expected Initial State |
|---|---|---|---|---|
| 1 | IT-005, IT-008 | FR-004, FR-008, FR-011 | AC-009, AC-027 | Migration 64 and registration quarantine shapes are absent |
| 2 | IT-004, IT-006, IT-016, IT-021, REG-006 | FR-004, FR-005, FR-009, NFR-001, RULE-004, RULE-007 | AC-005, AC-006, AC-011, AC-016, AC-027, AC-029 | Ownerless persistence, unified reservations, and atomic binding are absent |
| 3 | UT-002 | NFR-003, RULE-001 | AC-017, AC-022 | Registration provider configuration is absent |
| 4 | UT-003, UT-004 | FR-003, FR-015, RULE-005 | AC-023, AC-026 | Prompt capability and no-downgrade adapter are absent |
| 5 | IT-001, IT-006, UT-013 | FR-002, FR-005, FR-013, NFR-003, RULE-001, RULE-007 | AC-003, AC-006, AC-022, AC-029 | Registration route and request validation are absent |
| 6 | IT-002, IT-003, IT-018 | FR-003, FR-015, RULE-001, RULE-003, RULE-005 | AC-003, AC-004, AC-023, AC-026 | Federated start requires a known account |
| 7 | IT-007, IT-008, IT-009, IT-010, IT-020, IT-021 | FR-005, FR-007, FR-008, FR-009, FR-011, NFR-001, RULE-004 | AC-006, AC-008, AC-009, AC-011, AC-012, AC-027 | Callback requires prebound owner authority |
| 8 | IT-011, IT-012, IT-022, REG-005 | BR-001, FR-009, FR-010, FR-011, RULE-003 | AC-010, AC-011, AC-013 | Shared onboarding guards reject registration |
| 9 | UT-005, UT-014, IT-013, IT-014, IT-019 | FR-007, FR-011, NFR-004, RULE-006 | AC-012, AC-014, AC-025 | Only success or generic browser errors render |
| 10 | UT-001, UT-011, IT-017, REG-004, REG-007 | FR-002, FR-006, FR-013, NFR-001, NFR-002, NFR-004, RULE-002 | AC-003, AC-007, AC-009, AC-011, AC-012, AC-014, AC-015, AC-016, AC-022 | Flutter registration request and security inventory are absent |
| 11 | UT-007, UT-009 | FR-014, FR-015 | AC-024, AC-026 | Flutter errors and pending state are sign-in-only |
| 12 | UT-006, UT-008, UT-010 | FR-001, FR-006, FR-015 | AC-007, AC-020, AC-021, AC-026 | Controller has no registration operation |
| 13 | AT-001, AT-002, AT-003, UT-012 | BR-001, BR-002, FR-001, FR-006, FR-012, FR-014 | AC-001, AC-002, AC-007, AC-019, AC-020, AC-021, AC-024 | Both welcome actions route to sign-in |
| 14 | AT-006, AT-007 | FR-011, FR-015, NFR-004, RULE-006 | AC-014, AC-025, AC-026 | Error callbacks collapse to generic missing-code state |
| 15 | AT-004, AT-005, AT-008, AT-009, REG-001, REG-002, REG-003, REG-008, REG-009 | BR-001, BR-002, BR-003, FR-001, FR-009, FR-010, NFR-003, RULE-003 | AC-002, AC-010, AC-013, AC-017, AC-018, AC-020, AC-022 | Full additive integration is incomplete |
| 16 | REG-010 | FR-016 | AC-028 | Offline release inventory has not covered registration |
| Release | MAN-001 | BR-001, FR-003, FR-016, RULE-005 | AC-001, AC-010, AC-023, AC-028 | Manual live-provider evidence is intentionally absent during coding |

## Implementation Steps
### Step 1: IT-005, then IT-008
- Write failing test: `IT-005` added in `provider_registration_migration_test.go`; `IT-008` remains pending.
- Run command: `TEST_DATABASE_URL=... TEST_DATABASE_REQUIRED=true go test ./internal/db -run '^TestProviderRegistrationMigrationAuthorityShapesUpDownUp$' -count=1`.
- Confirmed failure: Missing `000064_provider_first_registration.up.sql`; after the first implementation attempt, the test also caught SQL three-valued logic accepting a partial owner triple.
- Implement: Added migration 61 registration purpose/provider/issuer fields, nullable all-or-none owner authority, purpose-aware constraints, partial owner index, and rollback deletion/restoration.
- Run command: Focused `IT-005` passed; `TEST_DATABASE_URL=... TEST_DATABASE_REQUIRED=true go test ./internal/db -count=1` passed.
- Refactor: Kept authority checks explicit and reset only test fixtures between up/down/up passes.
- Notes: `IT-005` and the migration slice of `IT-008` are green. Migration 64 now includes an ownerless protected credential quarantine and registration-only `cleanup_pending` state; callback fault-boundary coverage remains in Step 7.

### Step 2: IT-004, IT-006, IT-016, IT-021, REG-006
- Write failing test: `IT-004` and `IT-021` added separately in `store_test.go`. `IT-006`, `IT-016`, and remaining `REG-006` added as separate focused real-PostgreSQL tests in `auth_request_admission_test.go`.
- Run command: Focused commands selected each named test independently, ending with `... go test ./internal/auth -run '^TestProviderRegistrationPreservesLoginAndDeletionAmbiguityRetentionAndCapacity$' -count=1`.
- Confirmed failure: `IT-004` failed because registration purpose/context/provider metadata were absent; its first setup run also exposed that the focused auth schema lacked the pre-existing migration 37 constraints required by migration 61. `IT-006` failed to compile because reservation APIs and reservation-consuming registration persistence did not exist. `IT-016` failed to compile because registration-only exchange claim, exchange-expiry geometry, stale reconciliation, and cleanup-pending state did not exist. `IT-021` failed to compile because no atomic registration authority binding operation or binding value existed. `REG-006` failed behaviorally because admission counted the live registration reservation but sweeper pending metrics reported only the two retained owner-bound ambiguity rows.
- Implement: Added purpose-aware registration metadata, ownerless save behavior, provider/issuer persistence, nullable authority scanning, and migration 61 to the auth store fixture. Added durable expiring reservations under unified admission. For `IT-016`, added expiry-checked registration exchange claim and restart-safe stale reconciliation: a held quarantine atomically becomes cleanup-pending/pending, while a stale exchange without recoverable credentials becomes registration ambiguity. Registration ambiguity stops consuming capacity after `AuthRequestExpiry` and reaches terminal deletion after retention; login/deletion ambiguity behavior is unchanged. Admission and sweeps reclaim expired reservations/ready rows, and production exchange expiry is callback timeout plus one sweep interval. For `IT-021`, added one owner-fenced transaction that validates the current departed/active lifecycle, exact request attempt/issuer and held credential, attaches the complete owner triple, inserts the matching `pending_handoff` OAuth parent, and deletes quarantine. Stale lifecycle authority rolls back without partial attachment. For `REG-006`, aligned sweeper pending metrics with admission by including unexpired reservations and the same purpose-aware ambiguity interval.
- Run command: Focused `IT-004`, `IT-006`, `IT-016`, `IT-021`, and `REG-006` passed against required PostgreSQL. Neighbor sets passed after each implementation. Final focused race command for `IT-016`, `IT-021`, and `REG-006` passed, followed by `go test -race ./internal/auth ./internal/db ./internal/ownerlifecycle -count=1` with the required database.
- Refactor: Kept owner fencing unchanged for login/deletion and isolated the ownerless branch to registration.
- Notes: `IT-004`, `IT-016`, `IT-021`, `REG-006`, and the durable shared-capacity/store portion of `IT-006` are green. Login/deletion ambiguity admission and retention remain unchanged. Migration 64's one-to-one quarantine cascade required classifying `oauth_auth_requests` as a fixed terminal-purge parent; the owner-lifecycle inventory guard and full package passed. No registration route or rate-limiter work was added; the `RateClassAuth` route portion of `IT-006` remains pending with Step 5.

### Step 3: UT-002
- Write failing test: Added one table-driven `TestLoadConfigRegistrationProviderOrigin` in `internal/app/config_test.go` covering the absent production setting default, one controlled public HTTPS fixture override, and rejection of private, HTTP, path, query, and credential-bearing values.
- Run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/app -run '^TestLoadConfigRegistrationProviderOrigin$' -count=1`.
- Confirmed failure: The package failed to compile at the new assertion because `Config` had no `OAuthRegistrationProviderOrigin` field. This was the expected missing configuration behavior, not a fixture or setup failure.
- Implement: Added `OAuthRegistrationProviderOrigin url.URL` to `Config`; `LoadConfig` reads server-owned `OAUTH_REGISTRATION_PROVIDER_ORIGIN`, defaults it to `https://bsky.social`, and validates it through the existing canonical public HTTPS origin rules. Generalized that parser only enough to report the setting's own key while preserving existing `OAUTH_PUBLIC_ORIGIN` behavior.
- Run command: The focused command passed. `GOTOOLCHAIN=go1.26.6 go test ./internal/app -count=1` passed, and `GOTOOLCHAIN=go1.26.6 go test ./internal/app -run '^$' -count=1` passed as the nearby compile-only check.
- Refactor: Reused one key-aware canonical-origin helper instead of duplicating origin validation.
- Notes: UT-002 is green for NFR-003, RULE-001, AC-017, and AC-022. No OAuth adapter, route, provider wiring, environment documentation, or other test was added.

### Step 4: UT-003, UT-004
- UT-003 write failing test: Added `TestRegistrationAuthMetadataSelectsAdvertisedCreatePrompt` in `internal/auth/registration_oauth_test.go` for exact case-sensitive `create`, duplicate exact values, case variants, absent/empty/unrelated values, and malformed scalar/member metadata.
- UT-003 run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/auth -run '^TestRegistrationAuthMetadataSelectsAdvertisedCreatePrompt$' -count=1`.
- UT-003 confirmed failure: The focused package failed to compile because `decodeRegistrationAuthMetadata` did not exist.
- UT-003 implement: Added `RegistrationAuthMetadata`, JSON decode into an embedded pinned-Indigo `oauth.AuthServerMetadata`, Indigo `Validate(issuer)`, and exact case-sensitive membership selection in `registration_oauth.go`. Malformed prompt field types fail decoding; missing, empty, case-variant, and unrelated values omit the prompt.
- UT-003 green: The focused command passed. `appview/go.mod` and `go.sum` remained unchanged at pinned Indigo `v0.0.0-20260417172304-7da09df6081d`.
- UT-003 refactor: None; retained one decode function and one decision method.
- UT-004 write failing test: Added `TestRegistrationOAuthPromptedPARDoesNotDowngrade` in `internal/auth/registration_oauth_test.go`. Its table covers prompted generic 4xx, DPoP, scope, timeout, 429, and 5xx failures plus an ordinary unprompted 4xx; its nonce case executes pinned Indigo and locks prompt, absent login hint, PKCE, confidential-client assertion, DPoP regeneration, and same-form replay.
- UT-004 run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/auth -run '^TestRegistrationOAuthPromptedPARDoesNotDowngrade$' -count=1`.
- UT-004 confirmed failure: The focused package failed to compile because the registration adapter and bounded OAuth failure types did not exist.
- UT-004 implement: Added a registration-scoped shallow copy of Indigo's `oauth.ClientApp` and hardened `http.Client`; its transport decorates only the exact PAR form with `prompt=create`, rejects login hints/preexisting prompts, consumes provider-controlled failure bodies, and returns only stage/status/transience/prompted typed facts. Prompted rejection and transport/timeout/429/5xx map to `providerUnavailable`; an ordinary unprompted 4xx maps to `registrationIncomplete`. Only exact top-level `use_dpop_nonce` with HTTP 400 and `DPoP-Nonce` is passed to Indigo as sanitized JSON for its existing one replay.
- UT-004 green: The focused command passed. The first post-implementation run exposed pinned Indigo's rule that the successful replay response must repeat `DPoP-Nonce` for it to remain in `AuthRequestData`; the fixture was corrected without changing implementation. UT-003 then passed independently again. The combined focused tests passed under `-race`, and `TEST_DATABASE_URL=postgres://craftsky:dev@localhost:15871/craftsky_dev?sslmode=disable TEST_DATABASE_REQUIRED=true GOTOOLCHAIN=go1.26.6 go test -race ./internal/auth -count=1` passed against the running worktree PostgreSQL stack.
- UT-004 refactor: Removed an unnecessary error unwrap; retained one fixed redacted `Error()` string and no provider body/cause.
- Notes: Pinned Indigo remained unchanged; Dart/Flutter analysis was not applicable. Tracked plus untracked `git diff --check` checks passed. No route, federated flow, callback, or Flutter behavior was added.

### Step 5: IT-001, IT-006, UT-013
- IT-001 write failing test: Added `TestProviderRegistrationRouteContract` in `internal/routes/routes_test.go` for the exact versioned route, strict handoff-only body, no-store `{authUrl}`, server-owned provider, standard failure envelope/request ID, and absence of an unversioned route.
- IT-001 run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/routes -run '^TestProviderRegistrationRouteContract$' -count=1`.
- IT-001 confirmed failure: The valid request returned 404 because `POST /v1/auth/registrations` was not registered. The first green attempt then correctly showed that the route-only fixture did not inject production's outer request ID; the test was corrected to provide the run ID consumed by the standard envelope rather than weakening the assertion.
- IT-001 implement: Added the anonymous `/v1/auth/registrations` route and central policy, strict JSON handler with only `handoffMode`/`loopbackRedirectUri`, coordinator entry point, no-store success response, provider override rejection, and route/access/inventory declarations. `/v1/auth/login` was not changed.
- IT-001 green: The focused command passed.
- IT-001 refactor: Reused `decodeSingleJSON`, existing handoff constants/loopback pattern, standard envelope, and existing `{authUrl}` response type.
- IT-006 route write failing test: Added `TestProviderRegistrationRouteUsesAuthRateLimitAndCapacityOutcome` with the real local `RateClassAuth` limiter and a recording coordinator. It proves a repeated same-device request is rejected before a second provider call with bounded 429/`Retry-After`, then exercises the shared-store capacity error.
- IT-006 route run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/routes -run '^TestProviderRegistrationRouteUsesAuthRateLimitAndCapacityOutcome$' -count=1`.
- IT-006 route confirmed failure: The limiter assertions passed, including one provider call only, but `ErrAuthRequestCapacity` returned 502 `registration_provider_unavailable` instead of the shared 503 capacity outcome.
- IT-006 route implement: Mapped `ErrAuthRequestCapacity` to 503 `authentication_capacity_exhausted` with a bounded positive `Retry-After`, matching login. The existing route wrapper keeps device admission and `RateClassAuth` ahead of the handler/provider call.
- IT-006 route green: The focused command passed.
- UT-013 write failing test: Added `TestRegistrationRequestValidationDoesNotCallProvider` in `internal/auth/handlers_test.go`, exercising the handler through the real device middleware for missing/invalid device IDs, missing/invalid handoff modes, missing/invalid/mismatched loopback URIs, unknown handle/provider fields, and valid verified-link/loopback bodies without a handle.
- UT-013 run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/auth -run '^TestRegistrationRequestValidationDoesNotCallProvider$' -count=1`.
- UT-013 confirmed failure: A verified-link request carrying a loopback URI returned 200 and called the provider instead of failing handoff validation before provider work.
- UT-013 implement: Rejected `loopbackRedirectUri` unless `handoffMode` is exactly `loopback`; retained middleware-owned device syntax validation and strict unknown-field rejection.
- UT-013 green: The focused command passed; every invalid case made zero provider calls, while both valid handoff-only cases proceeded without handle validation.
- Step 5 focused verification: `go test ./internal/auth` for UT-013/capacity, `go test ./internal/routes` for IT-001/IT-006 plus policy/access/inventory, and `go test ./internal/app` for provider configuration all passed. Full non-race `go test ./internal/auth ./internal/routes ./internal/app -count=1` passed.
- Step 5 required-PostgreSQL race verification: The first `TEST_DATABASE_REQUIRED=true go test -race ./internal/auth ./internal/routes ./internal/app -count=1` run found the federated App test fixture had stopped at migration 38 and lacked migration 61's shared reservation table. Updated only that fixture's emulated migration-37 constraints and applied migration 61. The focused federated regression then passed, followed by the complete required-PostgreSQL race command passing for all three packages.
- Step 5 scope: IT-001, the route portion of IT-006, and UT-013 are complete. Provider discovery/PAR, actual production `StartRegistration`, callback completion, and Flutter remain pending by the explicit Step 5-only boundary.
- Notes: Read `docs/superpowers/specs/2026-04-21-appview-api-architecture-design.md` and the current workflow documents before edits. Production provider discovery/PAR remains Step 6; Step 5 adds only the route/start seam needed by recording fixtures.

### Step 6: IT-002, IT-003, IT-018
- IT-002 write failing test: Added `TestProviderRegistrationServerFirstDiscoveryAndPAR` to the controlled TLS federated fixture. It requires configured-provider protected-resource discovery, validated authorization metadata, one ordinary server-first PAR with client ID/exact scopes/state/PKCE/DPoP and no `login_hint`, the discovered authorization endpoint, and durable ownerless request state before return.
- IT-002 run command: `TEST_DATABASE_URL=... TEST_DATABASE_REQUIRED=true GOTOOLCHAIN=go1.26.6 go test ./internal/app -run '^TestProviderRegistrationServerFirstDiscoveryAndPAR$' -count=1`.
- IT-002 confirmed failure: The package failed to compile because no production registration adapter or `OAuthFlowServiceOptions` registration wiring existed. After minimum implementation, the fixture caught that owner-fenced `GetAuthRequestInfo` is intentionally unavailable for ownerless rows; the persistence assertion was corrected to inspect the protected database JSON directly rather than weakening store authority.
- IT-002 implement: Added production extended-metadata discovery over the hardened metadata client, registration adapter injection and configured provider wiring, and `StartRegistration` validation/reservation/discovery/PAR/atomic reservation-consuming persistence/redirect construction. Every pre-persistence return releases the reservation through one deferred cleanup.
- IT-002 green: The focused required-PostgreSQL command passed.
- IT-002 refactor: Reused Indigo's OAuth metadata types, validation, and PAR implementation plus the existing registration store context rather than adding parallel OAuth state; the adapter performs the two metadata fetches so it can retain the extended prompt field and bounded start-stage classification.
- IT-002 notes: IT-002 is green for FR-003, RULE-001, RULE-003, AC-003, and AC-004. No callback or Flutter behavior was added. IT-003 and IT-018 remain pending and will extend this same fixture one test ID at a time.
- IT-003 write focused test: Added `TestProviderRegistrationAdvertisedCreatePromptDoesNotDowngrade` to the production federated path. It covers advertised-prompt success with durable ready state and prompted generic/provider-text rejection with one PAR, no prompt-free retry, bounded provider-unavailable facts, no persisted request, and reservation release.
- IT-003 run command: `TEST_DATABASE_URL=... TEST_DATABASE_REQUIRED=true GOTOOLCHAIN=go1.26.6 go test ./internal/app -run '^TestProviderRegistrationAdvertisedCreatePromptDoesNotDowngrade$' -count=1`.
- IT-003 initial state: The focused integration test passed on its first run. This is recorded explicitly rather than misrepresented as red: UT-003/UT-004 had already implemented and tested advertised prompt selection, redaction, and no downgrade in Step 4, and IT-002 made that existing adapter reachable from production `StartRegistration`. No honest missing IT-003 behavior remained after IT-002.
- IT-003 implement/refactor: None. The existing Step 4 adapter plus IT-002 wiring satisfied the production integration contract without additional code.
- IT-003 notes: IT-003 is green for FR-003, FR-015, RULE-005, AC-023, and AC-026. IT-018 remains pending and is expected to drive missing discovery/metadata/start classification and exact API mapping.
- IT-018 write failing tests: Added `TestProviderRegistrationStartFailureClassificationDoesNotFallback` for advertised/absent prompt, provider text, invalid request/client/DPoP/scope, invalid metadata/endpoint, timeout, 429, 5xx, and the same-body DPoP nonce replay exception. Added `TestRegistrationHandlerMapsBoundedStartFailures` under the same IT-018 loop for exact `/v1/` error categories.
- IT-018 run commands: Required-PostgreSQL federated command selecting `TestProviderRegistrationStartFailureClassificationDoesNotFallback`, then `go test ./internal/auth -run '^TestRegistrationHandlerMapsBoundedStartFailures$' -count=1`.
- IT-018 confirmed failures: After correcting one test-only empty-string containment bug, invalid metadata and endpoint rejection escaped as generic unbounded errors instead of typed `registrationIncomplete`. The handler then failed independently because it mapped that bounded incomplete fact to `registration_provider_unavailable`.
- IT-018 implement: Added bounded discovery and authorization-metadata stages over the hardened metadata client, exact status/federated-boundary classification, bounded document reads, protected-resource and extended authorization metadata validation, and redacted typed failures. Destination/redirect/size/metadata/endpoint failures are incomplete; timeout/upstream/429/5xx are provider unavailable. The handler now maps typed incomplete starts to `registration_incomplete`; prompted rejection and transient failures retain `registration_provider_unavailable`.
- IT-018 green: The federated classification table and handler mapping tests passed independently. Every failed start released its reservation and persisted no request. Every PAR case made one logical request; only `use_dpop_nonce` made two HTTP attempts, with an identical form and regenerated DPoP handled by pinned Indigo.
- IT-018 refactor: Consolidated bounded metadata fetch/status handling in the registration adapter and retained provider bodies/causes outside returned errors.
- IT-018 notes: IT-018 is green for FR-003, FR-015, RULE-005, AC-023, and AC-026. No provider error-description parsing, prompt-free fallback, callback authority flow, or Flutter behavior was added.
- Step 6 focused verification: The three federated tests passed together against required PostgreSQL; UT-003/UT-004, IT-018 handler mapping, and UT-013 passed together; focused registration route contract/rate-capacity tests passed; `git diff --check` passed.
- Step 6 regression correction: The first combined auth check found the PAR-only Step 4 fixture intentionally had no metadata resolver. Adapter construction was narrowed back to PAR dependencies, while discovery now fails closed only when invoked without metadata dependencies. The focused auth set then passed.
- Step 6 timeout correction: Final read-back identified that a real overall start deadline would leave the deferred reservation release using an already-canceled context. The IT-018 timeout case was tightened to wait for the operation context deadline and failed with one retained reservation. Cleanup now uses a detached, five-second-bounded database context; the same focused timeout case passed with zero retained reservations.
- Step 6 required-PostgreSQL race verification: `TEST_DATABASE_URL=... TEST_DATABASE_REQUIRED=true GOTOOLCHAIN=go1.26.6 go test -race ./internal/auth ./internal/app ./internal/routes -count=1` passed after the final timeout correction (`auth` 9.166s, `app` 5.464s, `routes` 2.674s).
- Step 6 scope: Complete only for IT-002, IT-003, and IT-018. Production `OAuthFlowService.StartRegistration` is wired through validated config, the hardened registration adapter, shared reservation admission, and durable request persistence. Callback authority/completion (Step 7) and all Flutter work remain untouched.

### Step 7: IT-007, IT-008, IT-009, IT-010, IT-020, IT-021
- IT-007 write failing test: Added `TestProviderRegistrationCallbackProvesSeparatePDSAndIssuerAuthority` to the controlled TLS federated fixture. It starts an ownerless request and requires exact callback state/issuer/code and token subject validation, a concrete PDS on a different host from the issuer, authoritative PDS discovery back to the exact stored issuer, and a bound pending parent only after proof.
- IT-007 run command: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationCallbackProvesSeparatePDSAndIssuerAuthority$' -count=1`.
- IT-007 confirmed failure: `CompleteCallback` returned `invalid owner DID` because the production callback sent ownerless registration metadata into the existing prebound-owner `WithExistingAuth` path.
- IT-007 implement: Added registration callback dispatch, ownerless one-time exchange claim/read/final transitions, exact stored issuer/code checks, complete mandatory token/scope validation and token `sub` DID parsing, durable held quarantine before identity-network work, authoritative DID -> PDS -> discovered issuer proof, eligible owner fencing, and consumption of the exact quarantine through the existing atomic authority/pending-parent bind. PDS and issuer origins are compared by exact discovered issuer and may have different hosts.
- IT-007 green: The focused controlled-fixture command passed. It observed one token exchange, two protected-resource discoveries (configured provider plus returned DID's PDS), complete owner authority, and one `pending_handoff` parent.
- IT-007 refactor: Added explicit ownerless store methods rather than weakening owner-fenced login/deletion transaction helpers.
- IT-008 callback/fault write failing tests: Added `TestProviderRegistrationCallbackFaultBoundariesConverge` with controlled missing access/refresh tokens, missing/duplicate mandatory scopes, malformed token subject, DID lookup failure, missing PDS, mismatched discovered issuer, and forced quarantine-insert failure. Added `TestProviderRegistrationCredentialCleanupConverges` for held eligibility, cleanup claim, restart, revocation, and terminal state.
- IT-008 run commands: Required-PostgreSQL focused `go test ./internal/app -run '^TestProviderRegistrationCallbackFaultBoundariesConverge$' -count=1` and `go test ./internal/auth -run '^TestProviderRegistrationCredentialCleanupConverges$' -count=1`.
- IT-008 confirmed failures: Malformed token responses transitioned directly to ambiguity without retaining returned revocable tokens; authority failures left `exchange_started/held`; forced quarantine persistence failure left ambiguity with zero revocations; and the existing revocation worker processed zero ownerless credentials.
- IT-008 implement: Added complete token shape/scope validation before authority use; durable quarantine for every revocable malformed response; atomic held-to-pending/request-to-cleanup transition on token or authority rejection; immediate in-memory access/refresh revocation with failed-versus-ambiguous evidence when quarantine persistence fails; and ownerless credential claiming/retry/finalization in the existing bounded revocation worker. Held rows cannot be claimed before `eligible_at`; a restarted worker atomically changes an eligible held exchange to cleanup-pending, revokes, deletes the credential, and records `revoked`. Lock order remains request then quarantine.
- IT-008 green: Both focused required-PostgreSQL commands passed. Every table case created zero owners and zero parent sessions, and the DID lookup fixture observed a durable held credential before network work. The forced persistence fault made two revocation calls and ended `exchange_failed`; durable cleanup ended `revoked`, including after restart. Final review added blank and malformed scope-member cases; the blank-member case produced a second meaningful red because `strings.Fields` normalized it, then passed after exact RFC scope-token splitting/character validation.
- IT-008 refactor: Kept registration cleanup in the existing OAuth revocation processor and reused its lease/backoff/retention geometry rather than introducing a callback goroutine or second worker family.
- IT-009 write failing test: Added `TestProviderRegistrationCallbackEnforcesOwnerLifecycleEligibility` over absent, departed, active, deletion-pending, deleting, and terminal owners through the production controlled callback path.
- IT-009 run command: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationCallbackEnforcesOwnerLifecycleEligibility$' -count=1`.
- IT-009 confirmed failure: Absent/departed/active already bound under the exclusive onboarding fence, but deletion-pending, deleting, and terminal rejection left the request and quarantine at `exchange_started/held`.
- IT-009 implement: Preserved lifecycle rejection from `WithOnboardingAuth` and atomically released any still-held pre-bind credential to `cleanup_pending/pending`. A successful or already-bound path cannot be changed by that compensation CAS.
- IT-009 green: The focused required-PostgreSQL command passed for all six lifecycle states. Eligible rows carry the fenced generation/epoch; ineligible rows retain a null owner authority triple and pending cleanup credential.
- IT-009 refactor: None beyond one post-fence compensation point.
- IT-010 write failing test: Added `TestProviderRegistrationCallbackStateAndCodeAreOneTime` for duplicate exact callback parameters, two concurrent callbacks, and replay after successful binding.
- IT-010 run command: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationCallbackStateAndCodeAreOneTime$' -count=1`.
- IT-010 confirmed failure: Concurrent callback state already permitted only one database exchange, but duplicate `state`, `iss`, and `code` query values each completed successfully because `url.Values.Get` silently selected the first value.
- IT-010 implement: Required exactly one callback state globally and rejected duplicate registration issuer/code/error values before exchange. Missing or wrong issuer/code still claims and fails the trusted attempt; the authorization code is never retried.
- IT-010 green: The focused required-PostgreSQL command passed. The race produced one success, one token exchange, one pending parent, and a rejected replay; duplicate parameters made no token request.
- IT-010 refactor: Kept one exact callback parameter helper and did not change valid login/deletion callback behavior.
- IT-020 callback write failing test: Added `TestProviderRegistrationCallbackDeadlineCancelsDependenciesWithoutLateActivation` with blocking token and DID dependencies, a controlled short callback deadline, cancellation observation, durable state assertions, and a post-return no-late-owner/parent check.
- IT-020 callback run command: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationCallbackDeadlineCancelsDependenciesWithoutLateActivation$' -count=1`.
- IT-020 callback confirmed failure: Cancellation reached both dependencies, but token and DID timeout paths attempted terminalization with the expired operation context and remained `exchange_started` (the DID case retained a held quarantine).
- IT-020 callback implement: Added detached callback-finalization contexts bounded by the configured callback timeout for ownerless terminal/cleanup CAS operations. A canceled indeterminate token exchange becomes ambiguity; a canceled DID/PDS authority operation releases the durable credential to cleanup. Remote work still uses and observes only the original operation context.
- IT-020 callback green: The focused required-PostgreSQL command passed. Both dependencies observed cancellation promptly; token timeout ended ambiguous, DID timeout ended cleanup-pending/pending, and neither owner nor parent appeared after callback return.
- IT-020 callback refactor: Centralized detached registration finalization without changing login/deletion operation contexts.
- IT-021 integration-completion write failing test: Added `TestProviderRegistrationCallbackPublishesAtomicBoundAuthority`. Inside the production callback finalizer it requires the exact fenced authority in both callback metadata and attempt, a matching pending parent already committed, and no remaining quarantine.
- IT-021 integration-completion run command: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationCallbackPublishesAtomicBoundAuthority$' -count=1`.
- IT-021 integration-completion confirmed failure: The database authority triple, parent, and quarantine consumption were atomic, but the callback finalizer received the original ownerless/ready metadata with zero authority and no attempt ID.
- IT-021 integration-completion implement: Publish the exact candidate DID, fenced generation/auth epoch, exchange-started state, and attempt ID only after `BindRegistrationAuthority` commits the authority triple, matching pending parent, and exact held-credential deletion.
- IT-021 integration-completion green: The focused required-PostgreSQL command passed; callback metadata, attempt, request, and parent all carried the same authority while quarantine count was zero.
- IT-021 integration-completion refactor: None.
- Step 7 focused verification: The six controlled callback tests passed together in `internal/app`; the IT-008 cleanup and existing IT-021 atomic store race tests passed together in `internal/auth`; changed-file analysis reported no diagnostics; `git diff --check` passed.
- Step 7 required-PostgreSQL race verification: `TEST_DATABASE_REQUIRED=true go test -race ./internal/auth ./internal/app ./internal/federatedhttp ./internal/ownerlifecycle -count=1` passed after the final scope-token correction (`auth` 10.119s, `app` 8.898s, `federatedhttp` 1.235s, `ownerlifecycle` 17.666s).
- Step 7 scope: Complete only for IT-007, the IT-008 callback/fault boundaries, IT-009, IT-010, the IT-020 callback portion, and IT-021 integration completion. No trusted callback error rendering, shared onboarding/profile execution, handoff completion beyond the minimum pending parent bind, Flutter work, live-provider call, or commit was added.
- Notes: All requested Step 7 IDs and portions are green.

### Step 8: IT-011, IT-012, IT-022, REG-005
- IT-011 write failing test: Added `TestProviderRegistrationCompletesSharedOnboardingAndConfirmedHandoff` to the controlled federated fixture. It verifies a verified new DID uses optional Bluesky-profile read, missing Craftsky-profile initialization, identity-cache and repository-tracker effects, a durable pending parent/child handoff, and exact receipt confirmation before parent/child activation.
- IT-011 run command: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationCompletesSharedOnboardingAndConfirmedHandoff$' -count=1`.
- IT-011 confirmed failure: The callback reached verified atomic binding, then failed with `OAuth callback attempt invalid` when the ownerless registration attempt entered the existing login-only pending-onboarding guard.
- IT-011 implement: Added one narrow `permitsOrdinaryOnboarding` predicate naming only login and registration. Applied it to callback-attempt validity, pending-parent resume, profile initialization, handoff creation, and callback-handler onboarding dispatch. Account deletion remains explicitly separate and cannot enter profile or ordinary handoff behavior.
- IT-011 green: The focused required-PostgreSQL test passed. Before confirmation the OAuth parent and Craftsky child were `pending_handoff`/`pending_confirmation`; after exact token, receipt, and device confirmation both were `active`.
- IT-011 refactor: Reused one explicit purpose predicate rather than broad non-deletion checks.
- IT-012 write focused test: Added `TestProviderRegistrationAcceptsExistingOwnerAsNormalSignIn` for existing departed and active owners selected through provider-first OAuth. It completes callback and confirmation, asserts exactly one owner lifecycle row, and proves the lifecycle remains unchanged before confirmation.
- IT-012 run command: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationAcceptsExistingOwnerAsNormalSignIn$' -count=1`.
- IT-012 initial state: The focused test passed on its first run. This is recorded rather than misrepresented as red: IT-011's minimum explicit login-or-registration onboarding predicate necessarily removed the same purpose guard for both newly created and pre-existing verified DIDs. No newness branch or IT-012-specific implementation was added.
- IT-012 green: Existing departed and active owners each retained one lifecycle row, produced a provider-first pending parent, and completed the normal exact-confirmation handoff.
- IT-022 write failing tests: Added `TestProviderRegistrationPostBindingFailureAbandonsOnlyAttempt` for a new-owner profile failure and existing-owner handoff-preparation failure, plus `TestProviderRegistrationPendingParentPersistenceFailureLeavesCleanupOnly` for an injected atomic parent-insert failure.
- IT-022 run command: Required-PostgreSQL focused commands selecting each test independently, then together.
- IT-022 confirmed failure: Both post-binding fatal cases left the attempt parent `pending_handoff` and the auth request `exchange_started`; the existing owner's prior active session was therefore not enough to make the failed attempt safe.
- IT-022 implement: Registration now invokes the existing `AbandonPendingSession` compensation when its bound finalizer fails, exactly as login does. The compensation affects only the attempt parent/children and terminalizes the attempt request; the outer registration cleanup CAS remains safe after the quarantine has already been consumed.
- IT-022 green: New-owner failure retained only a departed owner; existing-owner failure preserved the active lifecycle and prior active session. Both attempt parents became `revocation_pending`, no attempt child became active, no quarantine remained after binding, and request evidence became terminal `consumed`. The injected pending-parent insert failure rolled back authority attachment/session persistence atomically and converged to owner `departed`, request `cleanup_pending`, credential `pending`, and zero parents.
- IT-022 refactor: Reused the established bounded parent-session cleanup path; no registration-specific revoker or deletion behavior was added.
- REG-005 write regression test: Added `TestOrdinaryOnboardingPreservesProfileAndAuxiliaryEffectSeverity` for both login and registration. It asserts profile failure stops before auxiliary effects, while cache/tracker failures each execute and return success.
- REG-005 run command: `go test ./internal/auth -run '^TestOrdinaryOnboardingPreservesProfileAndAuxiliaryEffectSeverity$' -count=1`.
- REG-005 initial state: The focused regression passed on its first run. No production change was made: existing login severity remained intact, and IT-011's explicit ordinary-onboarding predicate supplied registration parity without broadening deletion.
- REG-005 green: Profile initialization remains fatal for login and registration; identity-cache and repository-tracker errors remain warning-only for both.
- Step 8 focused verification: The four provider-registration tests passed together in `internal/app`; the new severity regression plus existing profile, handoff, and account-deletion selections passed in `internal/auth`; `git diff --check` passed.
- Step 8 required-PostgreSQL race verification: `TEST_DATABASE_REQUIRED=true go test -race ./internal/auth ./internal/app ./internal/ownerlifecycle -count=1` passed (`auth` 10.259s, `app` 9.416s, `ownerlifecycle` 17.307s).
- Step 8 scope: Complete only for IT-011, IT-012, IT-022, and REG-005. No trusted callback error rendering, Flutter, live-provider call, deletion behavior, or commit was added.
- Notes: Step 8 implementation and verification are complete. IT-012 and REG-005 were honest first-run greens because IT-011's minimum shared-purpose implementation necessarily covered their overlapping purpose guard; neither received speculative code.

### Step 9: UT-005, UT-014, IT-013, IT-014, IT-019
- UT-005 write failing test: Added `TestTrustedRegistrationFailureDestinationUsesOnlyStoredMetadata` for verified-link, loopback, and allowed development handoffs plus expired, consumed, and wrong-purpose metadata. Attacker callback destination values are supplied but must never affect the selected destination.
- UT-005 run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/auth -run '^TestTrustedRegistrationFailureDestinationUsesOnlyStoredMetadata$' -count=1`.
- UT-005 confirmed failure: The package failed to compile because durable metadata exposed no expiry and no trusted registration failure destination builder existed.
- UT-005 implement: Added the request expiry derived from stored `created_at` and configured expiry to loaded metadata. Added a destination builder that accepts only valid ownerless registration metadata in ready state before expiry and selects verified-link, exact stored loopback, or fixed allowed development destination. Callback values are deliberately ignored.
- UT-005 green: The focused command passed.
- UT-005 refactor: Kept destination authority in one handler-render helper and reused existing verified-link, loopback, and development URL validation.
- UT-014 write failing test: Added `TestRegistrationFailureCodeSerializesOnlyBoundedValues` for the three approved codes plus provider prose, URL, control-character, oversized, and empty values. It also requires a trusted failure to retain its internal cause while exposing only a fixed redacted `Error()`.
- UT-014 run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/auth -run '^TestRegistrationFailureCodeSerializesOnlyBoundedValues$' -count=1`.
- UT-014 confirmed failure: The package failed to compile because no bounded registration failure code or trusted failure type existed.
- UT-014 implement: Added `RegistrationFailureCode` with exactly `canceled`, `providerUnavailable`, and `registrationIncomplete`; JSON and text marshaling reject every other value. Added `TrustedRegistrationFailure` carrying validated stored metadata, a bounded code, and a private internal cause; `Error()` is fixed and redacted while `Unwrap()` retains internal error identity.
- UT-014 green: The focused command passed.
- UT-014 refactor: Kept code validation centralized and used it for every serialization path and trusted construction.
- IT-013 write failing test: Added `TestTrustedRegistrationFailuresReturnBoundedErrorsForEveryHandoffMode` through the real callback HTTP handler. It covers denial and post-state technical trusted failures for verified-link, loopback, and allowed development handoffs while injecting attacker destinations, callback codes, provider descriptions, URLs, and secret sentinels.
- IT-013 run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/auth -run '^TestTrustedRegistrationFailuresReturnBoundedErrorsForEveryHandoffMode$' -count=1`.
- IT-013 confirmed failure: Every trusted failure returned generic 400 browser HTML because the callback handler had no bounded failure branch.
- IT-013 implement: Extended callback rendering with a mutually exclusive bounded failure field. Verified-link and fixed development destinations receive only `error=<boundedCode>`; loopback posts exactly one `error` field to the stored URI. The callback handler recognizes only `TrustedRegistrationFailure`, derives the destination from its stored metadata, keeps no-store/CSP headers, and falls back to generic HTML if trusted rendering validation fails.
- IT-013 green: The focused command passed for every mode and both failure classes with no callback code, provider text, internal URL, secret, or attacker destination in output.
- IT-013 refactor: Reused the UT-005 trusted destination builder and existing contextual HTML/CSP rendering; success remains code-only.
- IT-014 write focused test: Added `TestUntrustedCallbackStateReturnsOnlyGenericLockedDownHTML` for missing, malformed, unknown, expired, consumed, and wrong-purpose state outcomes with attacker-controlled verified-link and loopback values.
- IT-014 run command: `GOTOOLCHAIN=go1.26.6 go test ./internal/auth -run '^TestUntrustedCallbackStateReturnsOnlyGenericLockedDownHTML$' -count=1`.
- IT-014 initial state: The focused test passed on its first run. This is recorded rather than misrepresented as red: the pre-existing generic callback error page already supplied locked-down no-store HTML, and UT-005 plus IT-013 had just restricted the new deep-link branch to validated trusted registration metadata.
- IT-014 implement/refactor: None beyond the preceding minimum trusted-branch validation. The test proves all untrusted variants receive generic 400 HTML with `default-src`, `script-src`, and `connect-src` set to `'none'`, no `Location`, no app URL, no loopback target, and no callback scripting.
- IT-019 write failing test: Added `TestProviderRegistrationAccessDeniedConsumesOnceWithoutSession` through the production provider-first flow, real PostgreSQL state, and callback HTTP handler. It invokes `access_denied` with provider description sentinels twice and inspects request state, provider traffic, OAuth sessions, Craftsky sessions, and both browser responses.
- IT-019 run command: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationAccessDeniedConsumesOnceWithoutSession$' -count=1`.
- IT-019 confirmed failure: The first denial correctly failed the durable attempt but returned generic 400 HTML instead of a trusted bounded cancellation handoff.
- IT-019 implement: After a valid ready-state load and successful one-time registration exchange claim, callback denial now terminalizes the request as `exchange_failed` and returns a trusted `canceled` failure. Other post-claim callback, token, authority, lifecycle, binding, and onboarding failures return trusted bounded incomplete/provider-unavailable errors while retaining their internal cause; pre-claim missing, stale, wrong-purpose, or replayed states remain untrusted. Provider descriptions are never used. Token timeout/cancellation and already-bounded transient OAuth failures map to provider unavailable; other technical completion failures map to registration incomplete.
- IT-019 green: The focused required-PostgreSQL command passed. The first denial returned only `error=canceled`, created no token request or OAuth/Craftsky session, and echoed no description. The row became `exchange_failed`; replay returned generic 400 HTML with no usable app destination.
- IT-019 refactor: Centralized post-claim callback category selection while retaining existing durable terminalization and cleanup paths.
- Step 9 focused verification: The four auth render/handler tests passed together with existing success code, account-deletion, and development-scheme callback regressions. IT-019 passed independently against required PostgreSQL. A trusted callback failure logs only fixed operation/result/category fields; no raw cause is attached.
- Step 9 required-PostgreSQL race verification: `TEST_DATABASE_REQUIRED=true go test -race ./internal/auth ./internal/app ./internal/routes -count=1` passed after final formatting (`auth` 10.986s, `app` 10.511s, `routes` 2.580s).
- Step 9 static/diff verification: Changed-file analysis reported no diagnostics and `git diff --check` passed. The focused diff was inspected for Step 9 traceability.
- Step 9 scope: Complete only for UT-005, UT-014, IT-013, IT-014, and IT-019. Success code rendering and account-deletion behavior remain on their existing branches; no Flutter, live-provider call, or commit was added.

### Step 10: UT-001, UT-011, IT-017, REG-004, REG-007
- UT-001 write failing test: Extended `app/test/auth/data/auth_api_client_test.dart` with verified-link, loopback, and development-scheme registration cases. Each captures the exact request map, rejects identity/provider/credential/token/code keys, and decodes the existing `{authUrl}` response.
- UT-001 run command: `just app-test test/auth/data/auth_api_client_test.dart`.
- UT-001 confirmed failure: The focused Flutter test failed to compile because `AuthApiClient.register` did not exist.
- UT-001 implement: Added `AuthApiClient.register`, posting only camelCase `handoffMode` and optional `loopbackRedirectUri` to `/v1/auth/registrations`, defaulting to the existing build-gated handoff policy, and parsing through the existing `LoginResponse` mapper.
- UT-001 green: The focused command passed all eight tests, including all three registration handoff variants.
- UT-011 write failing tests: Added `TestRegistrationStartLogsAndEnvelopeRedactSecrets` with authorization-code, PAR URI, access/refresh token, DPoP key, handoff, provider credential, and provider-error sentinels wrapped around a typed PAR failure. Added a Flutter interceptor test for the registration endpoint with provider-controlled 4xx text.
- UT-011 run commands: `go test ./internal/auth -run '^TestRegistrationStartLogsAndEnvelopeRedactSecrets$' -count=1` and `just app-test test/shared/api/providers/error_mapping_interceptor_test.dart`.
- UT-011 confirmed failures: AppView emitted no registration-start structured log. Flutter categorized `/v1/auth/registrations` as `appview.unknown` and would retain its 4xx backend message.
- UT-011 implement: Added bounded registration-start failure/success logs using only operation, stage, result/category, and run ID. Added the allowlisted Flutter endpoint category and suppressed backend message capture specifically for registration while retaining safe error code and request ID.
- UT-011 green: Both focused commands passed. Captured logs and the standard camelCase envelope omit every sentinel while retaining `registration.start`, `par`, `providerUnavailable`, and `request-registration-safe`; Flutter retains only `registration_incomplete`, `req_registration`, and `appview.auth.registrations`.
- IT-017 write failing test: Added a fixed-session Flutter interceptor case proving registration remains anonymous while retaining the stable device header. Added `TestProviderRegistrationCredentialBoundaryInventory` to the real federated fixture to inspect authorization/callback URLs, callback HTML, AppView logs, auth-request/parent persistence, provider credential sentinels, PDS token sentinels, generated DPoP private material, and the approved code-only handoff.
- IT-017 run commands: `just app-test test/shared/api/providers/session_auth_interceptor_test.dart` and required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationCredentialBoundaryInventory$' -count=1`.
- IT-017 confirmed failure: The fixed-session Flutter interceptor attached the Craftsky bearer to `/v1/auth/registrations`. The AppView sentinel inventory passed on its first run because Steps 4-9 already established redacted provider errors, server-side credential persistence, and code-only callback rendering.
- IT-017 implement: Added `/v1/auth/registrations` to the interceptor's exact anonymous-path allowlist; the existing all-path stable device header remains unchanged. No AppView production change was required by the integration inventory.
- IT-017 green: Both focused commands passed. Provider email/password never enter persistence; callback auth code, access/refresh tokens, and DPoP private material are absent from browser/log output; access/refresh/DPoP remain in AppView auth persistence; Flutter receives only the approved handoff URL and sends no bearer on registration start.
- REG-004 regression run: Ran the existing AppView callback rendering/security-header, exact-loopback real-listener, fixed development-scheme, route inventory/access/capability tests, plus Flutter build-gated handoff policy and handoff route secret-inventory tests unchanged.
- REG-004 initial state: All focused regressions passed on the first run; no missing behavior or production change was found.
- REG-004 green: Verified-link and debug-only development URLs contain only the short-lived code, loopback posts exactly one code field to the exact stored origin, error pages remain connection-denying, route inventory remains exact, and Flutter routes carry no bearer/PDS credential.
- REG-007 regression run: Ran all `internal/federatedhttp` tests under `-race` plus the real registration fixture's invalid metadata/endpoint and post-token authority-failure tables under `-race` with required PostgreSQL.
- REG-007 initial state: All regressions passed on the first run; no missing behavior or production change was found.
- REG-007 green: Private/mixed/rebound destinations are rejected before dial; every redirect is revalidated; OAuth metadata endpoints remain same-origin/public HTTPS; response bounds remain enforced; invalid registration metadata/endpoints and issuer/PDS authority failures remain fail-closed.
- Final Step 10 focused Flutter verification: `just app-test test/auth/data/auth_api_client_test.dart test/auth/data/oauth_handoff_mode_test.dart test/shared/api/providers/error_mapping_interceptor_test.dart test/shared/api/providers/session_auth_interceptor_test.dart test/router/oauth_handoff_route_test.dart` passed 26 tests. After analysis identified the existing `AuthApiClient` test fake as incomplete, a register stub was added without controller behavior; the expanded focused set including `auth_controller_test.dart` passed 44 tests.
- Final Step 10 AppView race verification: Required-PostgreSQL `go test -race ./internal/auth ./internal/app ./internal/federatedhttp -count=1` passed (`auth` 12.579s, `app` 11.740s, `federatedhttp` 1.820s).
- Final Step 10 analysis: Dependencies resolved, so `just app-analyze` was run. Its first pass found two null-aware-element lints, one long line, and the incomplete controller-test fake; all were corrected mechanically. The rerun passed with no issues.
- Refactor: Used Dart null-aware map entries for the optional loopback field, retained one exact anonymous path allowlist, and kept registration logs at the handler boundary with bounded typed values only.
- Step 10 scope: Complete only for UT-001, UT-011, IT-017, REG-004, and REG-007. No controller registration behavior, error model, pending-auth model, UI, localization, or live-provider behavior was implemented. No commit was created.

### Step 11: UT-007, UT-009
- UT-007 write failing test: Added `UT-007 maps only approved registration failure values` in `app/test/auth/models/auth_error_test.dart`. It covers exact trusted callback values, bounded registration-start API/network/browser-launch outcomes, arbitrary callback/provider/internal values, and exhaustive `AuthError` participation.
- UT-007 run command: `flutter test test/auth/models/auth_error_test.dart`.
- UT-007 confirmed failure: The focused test failed to compile because the strict local `RegistrationFailure` type and its callback/start boundary mappings did not exist.
- UT-007 implement: Added exactly `canceled`, `providerUnavailable`, and `registrationIncomplete` as a bounded `AuthError` enum. Exact camelCase callback values parse; known snake_case registration-start codes, network/server failures, and browser-launch failure map to their approved categories. Unknown/untrusted values and arbitrary errors return no category and retain no cause/detail.
- UT-007 green: The focused model test passed all three tests. The existing `lost confirmation response retries the durable receipt idempotently` controller test also passed independently, proving handoff transport failure remains `ServerUnavailable` with exact durable-receipt retry rather than entering registration mapping.
- UT-007 refactor: Kept callback parsing and start mapping as separate explicit constructors on the bounded value; no global API or handoff error behavior changed.
- UT-009 write failing tests: Added a registration model round-trip with no handle/session material and `UT-009 pause/resume stays retryable and a second start is allowed` in the pending provider suite. Existing sign-in model/provider tests were changed first to require the explicit sign-in purpose.
- UT-009 run command: `flutter test test/auth/models/pending_auth_test.dart test/auth/providers/pending_auth_provider_test.dart`.
- UT-009 confirmed failure: Both focused files failed to compile because `PendingAuthPurpose`, named `PendingAuth.signIn`/`registration` construction, and notifier `startSignIn`/`startRegistration` transitions did not exist.
- UT-009 implement: Added `PendingAuthPurpose.signIn/registration`, enforced a required parsed handle for sign-in and a null handle for registration, and added explicit synchronous notifier starts. Existing sign-in callers now use `startSignIn`. No lifecycle observer or cancellation inference was introduced; a second registration start simply replaces the local diagnostic hint and touches no session model/provider.
- UT-009 green: The focused model/provider command passed all seven tests, including pause/resume state identity, a permitted second registration start, strict purpose/handle serialization, and absence of token/code/receipt/session fields.
- UT-009 generation: Ran `dart run build_runner build --delete-conflicting-outputs` because the mappable model and Riverpod source changed. The installed generator reports that the delete option is removed/ignored; generation still succeeded. Retained only affected `pending_auth.mapper.dart`, `pending_auth_provider.g.dart`, and `auth_controller.g.dart` output; unrelated generator-only hash/whitespace churn was removed.
- Step 11 focused verification: `flutter test test/auth/models/auth_error_test.dart test/auth/models/pending_auth_test.dart test/auth/providers/pending_auth_provider_test.dart test/auth/providers/auth_controller_test.dart test/router/router_redirect_test.dart` passed all 45 tests. A final independent pending model/provider run passed all seven tests after adding explicit invalid purpose/handle assertions.
- Step 11 analysis: An initial `just app-analyze` run found four local lint issues, which were corrected. Two later attempts launched concurrently with Flutter tests failed before analysis while both Flutter processes contended over `ios/Flutter/ephemeral/Packages/.packages`; rerunning `just app-analyze` alone passed with no issues. A final `flutter analyze --no-pub` also passed with no issues.
- Step 11 diff verification: `git diff --check` passed. Existing Step 1-10 worktree changes remain intact; Step 11 changed only the bounded Flutter auth error model/test, pending-auth model/provider/generated outputs/tests, required sign-in call-site renames, and this execution record.
- Notes: UT-007 completed and was documented before UT-009 began. No controller registration operation, browser launcher behavior, completion page, UI, localization, or session creation was added.

### Step 12: UT-006, UT-008, UT-010
- UT-006 write failing test: Added `UT-006 registration launches once with pending set immediately before it` in `app/test/auth/providers/auth_controller_test.dart`. One focused loop covers successful launch, launcher `false`, and launcher throw while recording the registration API handoff arguments, exact URL, launch count, pending-state timing, and terminal controller state.
- UT-006 run command: `flutter test test/auth/providers/auth_controller_test.dart --plain-name "UT-006 registration launches once with pending set immediately before it"`.
- UT-006 confirmed failure: The focused file failed to compile because `AuthController.startRegistration` did not exist.
- UT-006 implement: Added `startRegistration` to await the local registry, reject a full registry before API work, call the dedicated registration API with its existing build-selected handoff fields, set pending registration immediately before invoking the existing external-application launcher once, and return to `AsyncData<void>` after a successful launch. Launcher `false` or throw clears pending and becomes bounded `registrationIncomplete`; bounded API start failures use the existing UT-007 mapping.
- UT-006 green: The focused command passed. The successful browser handoff remains non-loading and restartable; both launcher failure forms clear the unusable pending hint and expose no launcher detail.
- UT-006 refactor: Reused the existing API, pending notifier, registry, launcher provider, and bounded error model; no route, dialog, interstitial, Welcome, or Add Account UI was added.
- UT-006 notes: Complete for FR-006, AC-007, and AC-021. UT-008 and UT-010 remain pending and will run in that order. Preserve exact-receipt handoff recovery separately from fresh OAuth retry.
- UT-008 write failing test: Added `UT-008 renders bounded outcomes and retries only with fresh registration` in `app/test/auth/pages/auth_complete_page_test.dart`. It covers all three approved callback values, bounded localized copy, no automatic operation, explicit fresh registration, no handoff-code exchange on error callbacks, no Feed navigation after browser relaunch, and unknown internal-value redaction. The existing same-code transient handoff test remains the exact durable-receipt recovery proof.
- UT-008 run command: `flutter test test/auth/pages/auth_complete_page_test.dart --plain-name "UT-008 renders bounded outcomes and retries only with fresh registration"`.
- UT-008 confirmed failure: The first bounded callback rendered none of the approved category copy because every error callback fell through to the existing missing-code sign-in outcome. The first implementation run also caught an illegal provider mutation during `initState`; pending-state cleanup was moved to a post-frame callback.
- UT-008 implement: Strictly parsed only `canceled`, `providerUnavailable`, and `registrationIncomplete`; added generated localized category messages; cleared recognized pending UI state after the frame; never exchanges a code when any error is present; redacts unknown callback values behind the generic error; and wires each approved retry to a fresh `startRegistration`. Feed navigation is now gated to successful nonempty handoff-code completion, so a successful fresh browser launch from the error page cannot navigate.
- UT-008 generation: Ran `flutter gen-l10n`; only the localization source and generated localization outputs for the three new keys were retained.
- UT-008 green: The focused test passed, followed by the complete `auth_complete_page_test.dart` file passing all six tests. The existing transient handoff test still retries the same code, preserving controller-side durable receipt detection and exact confirmation retry.
- UT-008 refactor: Kept fresh OAuth retry and `_complete` handoff retry as separate callbacks. No auto retry, technical callback value, registration route, Welcome UI, or Add Account UI was added.
- UT-008 notes: Complete for FR-015 and AC-026. UT-010 remains pending.
- UT-010 write focused test: Added `UT-010 registry limit remains authoritative before start and after race` in `app/test/auth/providers/auth_controller_test.dart`. It proves five retained accounts reject before registration API/browser work, then starts at four, fills the fifth slot locally, and completes separate existing-DID and new-DID handoffs with full snapshot and confirmation assertions.
- UT-010 run command: `flutter test test/auth/providers/auth_controller_test.dart --plain-name "UT-010 registry limit remains authoritative before start and after race"`.
- UT-010 initial state: The focused test passed on its first run. This is recorded rather than misrepresented as red: UT-006 necessarily added the requested pre-start `<5` controller check, while the pre-existing `SessionRegistry.stageHandoff` and `upsertAndActivate` algorithms already gave duplicate-DID replacement priority over capacity and rejected a new sixth DID before mutation.
- UT-010 implement/refactor: None after the focused test. The registry remains the race authority; no account eviction, displacement, capacity algorithm, or session-registry production code changed.
- UT-010 green: At five, registration made zero API and launcher calls and exposed the existing `AccountLimitReached` outcome. In the four-to-five race, an existing DID replaced and activated its session at count five; a new DID preserved the byte-equivalent full snapshot, made no confirmation call, and returned `AccountLimitReached`.
- UT-010 notes: Complete for FR-001 and AC-020. UI disabling remains assigned to Step 13; no Welcome or Add Account registration UI was added in Step 12.
- Step 12 focused verification: `flutter test test/auth/providers/auth_controller_test.dart test/auth/pages/auth_complete_page_test.dart test/auth/models/session_registry_test.dart test/auth/models/auth_error_test.dart test/auth/models/pending_auth_test.dart test/auth/providers/pending_auth_provider_test.dart` passed all 46 tests, including existing exact receipt, storage recovery, sign-in, sign-out, pending-state, and registry regressions.
- Step 12 analysis: The first `just app-analyze` run found two `only_throw_errors` lints in the broad unknown-start rethrow and launcher test fake. The fake was narrowed to `Error`, and unknown start failures now preserve identity/stack through `Error.throwWithStackTrace`. The complete focused suite passed again, then `just app-analyze` passed with no issues.
- Step 12 diff verification: `git diff --check` passed. Step 12 changed only the controller/completion behavior and tests, three localization keys plus generator output, and this execution record on top of the existing Step 1-11 worktree.
- Step 12 scope: Complete only for UT-006, UT-008, and UT-010 in that strict order. No Welcome registration action, Add Account registration action, registration interstitial/route, registry eviction behavior, live-provider call, or commit was added.

### Step 13: AT-001, AT-002, AT-003, UT-012
- AT-001 write failing test: Replaced the old Welcome render smoke test with `AT-001 starts registration directly from Welcome`. It requires the exact disclosure before “Create an account”, direct controller dispatch, and no dialog or route replacement.
- AT-001 run command: `flutter test test/auth/welcome_page_test.dart --plain-name "AT-001 starts registration directly from Welcome"`.
- AT-001 confirmed failure: Welcome rendered no Bluesky portability disclosure and still exposed the old PDS action that routed to the handle form.
- AT-001 implement: Added the planned presentation-only `RegistrationAction`, generated an exact English disclosure/action localization, and wired Welcome directly to `AuthController.startRegistration` without a route, handle input, dialog, or interstitial. The existing controller immediately launches the returned URL through the external-application launcher proved by UT-006.
- AT-001 generation/green: Ran `flutter gen-l10n`; the focused AT-001 command then passed.
- AT-001 refactor: Shared presentation is isolated from controller access; Welcome owns the callback as planned.
- AT-001 notes: Complete for BR-001, FR-001, FR-006, FR-012, AC-001, AC-019, and AC-021. AT-002 remains next; exact two-surface localization coverage remains UT-012.
- AT-002 write failing tests: Added `AT-002 Sign in still opens the existing handle form` and `AT-002 Add Account keeps both paths subject to the limit`. They cover signed-out handle routing, below-limit handle and registration dispatch, and direct Add Account rendering at the five-account boundary.
- AT-002 run command: `flutter test test/auth/welcome_page_test.dart test/auth/sign_in_page_test.dart --plain-name "AT-002"`.
- AT-002 confirmed failure: The signed-out Sign in route/form passed unchanged, while Add Account had no `RegistrationAction` or create dispatch.
- AT-002 implement: `SignInPage(addAccount)` now watches the existing session registry, keeps the existing handle field/Continue action, renders the shared registration presentation, and disables the field plus both actions while registry state is unavailable, auth is busy, or five accounts are retained. Ordinary `SignInMode.signIn` remains handle-only.
- AT-002 green: Both focused acceptance tests passed. The existing account-switcher limit regression `UT-018 current semantics and five-account helper are explicit` also passed independently, proving Add Account itself remains unavailable from the switcher at five.
- AT-002 refactor: Reused the registry's existing `maxRetainedAccounts` authority and the shared presentation widget; no route, account-limit algorithm, or handle-login controller behavior changed.
- AT-002 notes: Complete for BR-002, FR-001, FR-012, AC-002, AC-020, and AC-021. AT-003 remains next.
- AT-003 write focused tests: Added `AT-003 resume without a callback permits an explicit fresh start` to the real controller suite and `AT-003 resume without a callback leaves Welcome retryable` to the widget suite. They pause/resume after a successful external launch, require non-loading registration state with no cancellation/dialog, and explicitly start a second request.
- AT-003 run command: `flutter test test/auth/welcome_page_test.dart test/auth/providers/auth_controller_test.dart --plain-name "AT-003"`.
- AT-003 initial state: Both focused acceptance tests passed on their first run. This is recorded rather than misrepresented as red: UT-006 already made successful external launch return to non-loading state, UT-009 deliberately added no lifecycle cancellation observer and allowed replacement pending state, and AT-001 made that controller operation reachable from Welcome.
- AT-003 implement/refactor: No production change was needed. The new controller test proves two explicit starts make two registration API calls and two external launches; the widget test proves pause/resume leaves “Create an account” enabled with no dialog.
- AT-003 green: Both focused tests passed together.
- AT-003 notes: Complete for FR-006, FR-014, AC-007, and AC-024. UT-012 remains next.
- UT-012 write focused tests: Added one rendered-surface test in each of `welcome_page_test.dart` and `sign_in_page_test.dart`. Both require the exact sentence “Bluesky hosts your portable account, which you can use with Craftsky.” and verify it appears before “Create an account”.
- UT-012 run command: `flutter test test/auth/welcome_page_test.dart test/auth/sign_in_page_test.dart --plain-name "UT-012"`.
- UT-012 initial state: Both focused tests passed on their first run. This is recorded honestly: AT-001 had to introduce and generate the exact localization to satisfy its disclosure requirement, and AT-002 reused the same presentation-only widget on Add Account, necessarily making both UT-012 surfaces exact before the dedicated localization lock was added.
- UT-012 implement/refactor: No further production change was needed. `RegistrationAction` remains the single generated-localization consumer for both surfaces.
- UT-012 green: Both focused tests passed together.
- UT-012 notes: Complete for FR-012, AC-019, and AC-021. Step 13 implementation is complete; focused widget/controller/router verification and app analysis follow without entering Step 14 or requesting review.
- Step 13 focused verification: `flutter test test/auth/welcome_page_test.dart test/auth/sign_in_page_test.dart test/auth/providers/auth_controller_test.dart test/auth/providers/pending_auth_provider_test.dart test/router/router_redirect_test.dart test/router/app_shell_account_switcher_test.dart` passed all 62 tests. This covers both entry widgets, immediate/repeated controller launch, pending lifecycle behavior, signed-out/sign-in/add-account routing, and existing account-switcher capacity behavior.
- Step 13 analysis: The first `just app-analyze` run found only one import-order and one line-length issue in the new tests. After mechanical fixes, the two complete widget files passed all nine tests, `flutter analyze` passed with no issues, and the final affected UT-012 test passed independently.
- Step 13 diff verification: `git diff --check` passed. Localization output was produced with `flutter gen-l10n`; no build-runner generation, route generation, Step 14 behavior, review, or commit was performed.
- Step 13 scope: Complete only for AT-001, AT-002, AT-003, and UT-012. Welcome starts registration directly; Sign in remains handle-first; Add Account provides both paths below the limit and disables them at the limit; resume without callback remains explicitly retryable; both surfaces share the exact localized disclosure.

### Step 14: AT-006, AT-007
- AT-006 write focused acceptance tests: Added one exact bounded-enum assertion and one typed-router widget scenario per approved outcome in `router_redirect_test.dart`. The scenarios require exactly `canceled`, `providerUnavailable`, and `registrationIncomplete`; exact safe localized copy; no issuer/DID/token/lifecycle text; no callback-code exchange; one explicit fresh-registration retry; and no Feed navigation after retry.
- AT-006 run command: `flutter test test/router/router_redirect_test.dart --plain-name "AT-006"`.
- AT-006 initial state: The first table-shaped router test reused global navigator keys across multiple router instances and then expected shortened provider-unavailable copy. Those failures were test isolation/assertion defects, not missing product behavior. After splitting each outcome into its own widget test and locking the generated exact copy, all four AT-006 tests passed without a production change. This is recorded rather than misrepresenting setup failures as a behavioral red: UT-007/UT-008 and Step 12 had already implemented the bounded model, completion presentation, fresh-registration retry, and Feed guard.
- AT-006 implement/refactor: No production change was required. Added only the router-bound acceptance fixture/controller and kept the existing completion widget implementation unchanged.
- AT-006 green/regressions: The focused AT-006 command passed four tests. `flutter test test/auth/pages/auth_complete_page_test.dart test/router/router_redirect_test.dart` passed all 25 tests, including same-code transient handoff recovery, `account_deletion_pending`, successful code exchange/navigation, and signed-in/signed-out callback routing.
- AT-006 notes: Complete for FR-011, FR-015, NFR-004, AC-014, and AC-026. AT-007 remains pending and starts only after this update. The existing durable handoff receipt/code recovery remains separate from fresh registration retry.
- AT-007 write focused acceptance test: Added `AT-007 unknown Flutter callback never exchanges or navigates to Feed` at the typed-router boundary. It supplies both an unknown technical error value and a handoff code, then requires generic bounded copy, technical-value redaction, no retry action, no handoff exchange, no fresh registration start, and no Feed navigation.
- AT-007 run command: `flutter test test/router/router_redirect_test.dart --plain-name "AT-007 unknown Flutter callback never exchanges or navigates to Feed"`.
- AT-007 initial state: The focused Flutter test passed on its first run. This is recorded honestly rather than claiming a red: UT-008 had already made every error callback return before code exchange, rendered unknown values generically, omitted retry, and gated Feed navigation to successful nonempty code completion.
- AT-007 implement/refactor: No production change was required. The acceptance test locks the existing Flutter boundary without changing successful code flow or `account_deletion_pending`.
- AT-007 AppView evidence: `GOTOOLCHAIN=go1.26.6 go test ./internal/auth -run '^(TestTrustedRegistrationFailureDestinationUsesOnlyStoredMetadata|TestTrustedRegistrationFailuresReturnBoundedErrorsForEveryHandoffMode|TestUntrustedCallbackStateReturnsOnlyGenericLockedDownHTML|TestCallbackRenderingCarriesOnlyCodeAndSecurityHeaders|TestCallbackRenderingAllowsOnlyExactLoopbackOrigin|TestDevelopmentSchemeCallbacksUseOnlyFixedCodeAndDeletionProofURLs)$' -count=1` passed. Trusted stored registration metadata alone selects verified-link, exact loopback, or fixed allowed-development handoff and carries only a bounded error. Missing, malformed, unknown, expired, consumed, and wrong-purpose state returns generic no-store HTML with connection-denying CSP, no `Location`, no Flutter URL, and no callback-supplied destination.
- AT-007 green/refactor: Focused Flutter and AppView render tests pass; no AppView source or database code changed in Step 14, so no required-database run is triggered by this step.
- AT-007 notes: Complete for FR-011, RULE-006, and AC-025. Step 14 implementation is complete; final focused completion/router verification and app analysis follow without entering Step 15, review, or commit.
- Step 14 final Flutter verification: `flutter test test/auth/pages/auth_complete_page_test.dart test/router/router_redirect_test.dart test/router/oauth_handoff_route_test.dart` passed all 28 tests. This includes all three bounded outcomes and explicit fresh retry, unknown/error no-exchange/no-Feed behavior, exact same-code handoff retry, successful code handoff/navigation, `account_deletion_pending`, callback route availability for signed-in and signed-out states, and code-only route serialization.
- Step 14 final analysis/format: `just app-analyze` passed with no issues. `dart format --output=none --set-exit-if-changed test/router/router_redirect_test.dart` reported zero changes.
- Step 14 scope: Complete only for AT-006 then AT-007. Step 14 added router acceptance coverage and this execution evidence; it required no production, AppView, schema, generated, or localization change. No required-database run, review, commit, or Step 15 work was performed.

### Step 15: AT-004, AT-005, AT-008, AT-009, REG-001, REG-002, REG-003, REG-008, REG-009
- AT-004 coverage review: Existing `TestProviderRegistrationCompletesSharedOnboardingAndConfirmedHandoff` is already the focused controlled federated acceptance fixture. It starts ownerless provider-first OAuth for a new DID, proves returned-DID authority, creates the departed owner and pending OAuth parent, performs optional Bluesky-profile read plus missing Craftsky-profile initialization, requests identity-cache update and repository tracking, creates/exchanges the code-only handoff, and asserts parent/child remain pending until exact confirmation activates both.
- AT-004 first run: `TEST_DATABASE_URL=... TEST_DATABASE_REQUIRED=true GOTOOLCHAIN=go1.26.6 go test ./internal/app -run '^TestProviderRegistrationCompletesSharedOnboardingAndConfirmedHandoff$' -count=1` passed on the first run.
- AT-004 implement/refactor: None. Existing focused behavior coverage is sufficient for BR-001, FR-010, and AC-010; adding a second test would duplicate the same public flow without increasing fault or boundary coverage.
- AT-004 notes: Honest first-run green. No source or test file changed for this ID. AT-005 starts only after this evidence update.
- AT-005 coverage review: Existing `TestProviderRegistrationAcceptsExistingOwnerAsNormalSignIn` covers provider-first selection of both departed and active existing owners, unchanged lifecycle authority, one owner row, normal pending-parent handoff, exact confirmation, and active session completion without any DID-newness requirement. Existing Flutter `UT-010 registry limit remains authoritative before start and after race` covers the local retained-account half: a returned DID already present at the five-account limit replaces and activates its session without increasing count.
- AT-005 first runs: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationAcceptsExistingOwnerAsNormalSignIn$' -count=1` and `flutter test test/auth/providers/auth_controller_test.dart --plain-name 'UT-010 registry limit remains authoritative before start and after race'` both passed on their first runs.
- AT-005 implement/refactor: None. Together the focused AppView and Flutter tests already cover FR-001, FR-009, RULE-003, AC-013, and the existing-DID part of AC-020 without duplicating lower-level fixture setup.
- AT-005 notes: Honest first-run green. No source or test file changed for this ID. AT-008 starts only after this evidence update.
- AT-008 coverage review/test change: The existing full handle-first `TestRealFederatedOAuthSessionAndPDSFlowsUsePurposeClients` covered handle resolution, callback, owner/session lifecycle, pending PDS use, active-session PDS reads/writes/uploads, backfill, and revocation, but did not lock the additive registration boundary at PAR. Tightened that same fixture to assert the pre-resolved owner DID is sent as `login_hint` and `prompt` remains absent; no second federated fixture was added.
- AT-008 first run: Required-PostgreSQL `go test ./internal/app -run '^TestRealFederatedOAuthSessionAndPDSFlowsUsePurposeClients$' -count=1` passed on the first run with the new assertions. `flutter test test/auth/providers/auth_controller_test.dart --plain-name 'signIn trims handle + @ prefix and posts to /login'` and focused existing profile/code-only callback regressions also passed first run.
- AT-008 implement/refactor: Test-only assertion strengthening in `federated_real_flow_integration_test.go`; no production fix was needed. The fixture continues to validate the token subject as the pre-resolved owner through the unchanged login callback path and completes its established lifecycle/session/PDS flow.
- AT-008 notes: Honest first-run green for BR-002 and AC-018. AT-009 starts only after this evidence update.
- AT-009 coverage review: Existing focused `UT-010 registry limit remains authoritative before start and after race` already models the complete acceptance race: registration starts with four retained accounts, another account fills slot five, then separate existing-DID and new-DID handoffs run through controller exchange/staging/confirmation behavior.
- AT-009 first run: `flutter test test/auth/providers/auth_controller_test.dart --plain-name 'UT-010 registry limit remains authoritative before start and after race'` passed on the first AT-009 run.
- AT-009 implement/refactor: None. The existing test asserts the retained DID is replaced/activated at count five, while a new sixth DID preserves the complete pre-handoff registry snapshot, is not retained, makes no confirmation call, and returns `AccountLimitReached`. Additional coverage would duplicate the exact FR-001/AC-020 race.
- AT-009 notes: Honest first-run green. No source or test file changed for this ID. REG-001 starts only after this evidence update.
- REG-001 coverage/run: Existing login handler, Flutter API/controller, and strengthened real federated login coverage are sufficient. Required-PostgreSQL `TestRealFederatedOAuthSessionAndPDSFlowsUsePurposeClients`, focused `TestLogin_(MissingHandle|MalformedHandle|AcceptsCamelCaseBody|RejectsSnakeCaseBody)`, and the complete Flutter `auth_api_client_test.dart` plus `auth_controller_test.dart` all passed.
- REG-001 first-run note: The first attempted Flutter `--plain-name` value used a literal pipe and selected zero tests; it was a command-filter error, not a behavioral red. The corrected two-file command passed all 31 tests. AppView commands passed first run.
- REG-001 implement/refactor: None beyond the AT-008 test-only PAR assertion already recorded before this ID. Login still requires the camelCase handle request, resolves it before OAuth, sends that DID as `login_hint`, omits a registration prompt, and does not consult the configured registration provider.
- REG-001 notes: Green for BR-002, AC-002, and AC-018. REG-002 starts only after this evidence update.
- REG-002 coverage review/test: No focused test previously proved that handle-first callback retains its pre-resolved subject equality rule after adding provider-first returned-DID authority. Added `TestHandleFirstOAuthRejectsMismatchedTokenSubject` to the existing controlled federated fixture.
- REG-002 first run: Required-PostgreSQL `go test ./internal/app -run '^TestHandleFirstOAuthRejectsMismatchedTokenSubject$' -count=1` passed on the first run.
- REG-002 implement/refactor: Test only; no production fix. A token response whose `sub` differs from the owner DID bound before authorization returns `ErrOAuthFlowInvalid`, never invokes the callback finalizer, creates zero OAuth sessions, and records the existing bounded `exchange_ambiguous` outcome. It does not enter registration authority discovery.
- REG-002 notes: Honest first-run green for BR-002 and AC-018. REG-003 starts only after this evidence update.
- REG-003 coverage/run: Existing deletion reauthentication, callback-purpose, atomic store, and migration-shape tests directly cover the regression. Focused `TestAccountDeletionReauthenticationIsFreshOwnerBoundAndSingleUse`, `TestOAuthCallbackUsesDeletionOnlyPurposeWithoutMintingOrdinaryAccess`, `TestLoginOAuthCallbackFailsClosedBeforeOrdinaryAccessWhenOwnerPendingDeletion`, required-PostgreSQL `TestStoreSavesAccountDeletionAuthRequestMetadataAtomically`, and required-PostgreSQL migration up/down/up all passed first run.
- REG-003 implement/refactor: None. Deletion requests retain non-null owner generation/auth epoch, exact `accountDeletion` purpose, owner/job binding, one-time proof and exact handle checks, and a callback branch that cannot mint ordinary PDS/Craftsky access. Migration 64 still rejects deletion rows with registration metadata or nullable/partial owner authority.
- REG-003 notes: Honest first-run green for BR-002 and AC-018. No source or test file changed for this ID. REG-008 starts only after this evidence update.
- REG-008 coverage/run: Ran the complete focused Flutter registry/controller/session/account-boundary/account-switch set: `session_registry_test.dart`, `auth_controller_test.dart`, `account_session_registry_provider_test.dart`, `auth_session_provider_test.dart`, `account_boundary_provider_test.dart`, `account_switch_routing_test.dart`, and `app_shell_account_switcher_test.dart`. All 58 tests passed first run.
- REG-008 implement/refactor: None. Existing coverage proves durable pending-handoff staging before activation, exact-receipt retry after lost confirmation, restart recovery without a browser code, coalesced concurrent recovery, storage-failure rollback, five-account behavior, Add Account preservation/activation, retained-session restoration, and account-switch boundary invalidation.
- REG-008 notes: Honest first-run green for FR-001, FR-010, AC-010, and AC-020. No source or test file changed for this ID. REG-009 starts only after this evidence update.
- REG-009 coverage review/test: Existing configuration, route, and Flutter serialization tests proved server ownership/no provider body, but no fixture completed the same lifecycle/session/handoff flow for two configured provider origins. Added `TestProviderRegistrationLifecycleAndHandoffAreNeutralAcrossConfiguredOrigins` and only the TLS fixture seams needed for `https://pds.real-flow.test` plus `https://pds-second.real-flow.test`.
- REG-009 first run: Required-PostgreSQL `go test ./internal/app -run '^TestProviderRegistrationLifecycleAndHandoffAreNeutralAcrossConfiguredOrigins$' -count=1` passed on the first run. The complete Flutter `auth_api_client_test.dart` and AppView `TestProviderRegistrationRouteContract` also passed first run.
- REG-009 implement/refactor: Test-fixture only; no production fix. Each configured origin is durably bound at registration start and performs its own protected-resource discovery/PAR initiation. Both then produce the same exact issuer proof, verified PDS host, profile initialization, cache/tracker effects, departed-to-active owner transition, pending-to-active OAuth/Craftsky session states, and code/exchange/receipt-confirmation handoff. The second configured start origin is absent from provider-neutral OAuth session data. Flutter sends only handoff fields, and the route rejects provider input.
- REG-009 notes: Honest first-run green after adding missing coverage for BR-003, NFR-003, AC-017, and AC-022. Step 15 ID loops are complete in the required order; final required race, Flutter, analysis, and diff checks follow without entering REG-010 or MAN-001.
- Step 15 required AppView race verification: `TEST_DATABASE_URL=... TEST_DATABASE_REQUIRED=true GOTOOLCHAIN=go1.26.6 go test -race ./internal/auth ./internal/app ./internal/federatedhttp ./internal/db ./internal/routes ./internal/ownerlifecycle ./cmd/appview -count=1` passed all seven packages (`auth` 12.359s, `app` 12.895s, `federatedhttp` 1.329s, `db` 6.180s, `routes` 4.567s, `ownerlifecycle` 22.014s, `cmd/appview` 4.583s).
- Step 15 focused Flutter verification: `just app-test test/auth test/shared/api/providers test/router/router_redirect_test.dart test/router/oauth_handoff_route_test.dart test/router/app_shell_account_switcher_test.dart test/router/account_switch_routing_test.dart` passed all 162 tests.
- Step 15 full Flutter verification: The first `just app-test` attempt reached 1,508 passing tests but exceeded the tool's 120-second timeout; SIGTERM produced shutdown-only incomplete-test errors. Re-running the same command with a five-minute limit passed all 1,612 tests in 1m55s.
- Step 15 analysis/diff verification: `just app-analyze` passed with no issues. `gofmt` was applied to the changed Go fixture, and `git diff --check` passed. Worktree status was inspected without changing or reverting prior Step 1-14 work.
- Step 15 scope: Complete only for AT-004, AT-005, AT-008, AT-009, REG-001, REG-002, REG-003, REG-008, and REG-009 in that exact order. Added only the missing handle-first PAR/subject and two-provider fixture assertions; no production code was required. REG-010, MAN-001/live Bluesky, implementation review, and commit were not run.

#### Step 15 Completion
- [x] Requested IDs executed in the required order with per-ID evidence
- [x] Full new-account and existing-account fixture paths verified
- [x] Handle-first login, `login_hint`, token subject, and deletion exact-purpose regressions verified
- [x] Account-limit race and multi-account recovery verified
- [x] Two configured provider origins produce provider-neutral lifecycle/session/handoff outcomes
- [x] Required PostgreSQL race packages pass
- [x] Focused and full relevant Flutter tests plus analysis pass
- [x] REG-010 and live MAN remain untouched

### Step 16: REG-010
- Coverage review: The repository has no `.github/workflows` directory. The release-equivalent AppView gate is `scripts/appview-check`, reached by `just appview-check`, and runs both ordinary and race `go test -count=1 ... ./...`; Flutter's automated gate is the `just app-test` recipe over `app/test`. The controlled registration integration fixture uses only `https://pds.real-flow.test`, `https://pds-second.real-flow.test`, and `https://auth.real-flow.test`, with an explicit resolver failure for every unknown hostname. Existing live Bluesky URL literals occur only as inert production-default, auth-store, admission, and migration values; no Go or Dart automated test contains the account-creation XRPC.
- Write focused inventory test: Added `TestReleaseGatesUseFixturesInsteadOfLiveBluesky` in `appview/internal/app/release_gate_inventory_test.go`. It inventories AppView tests, Flutter tests, release scripts, the `justfile`, and any future CI workflow YAML; rejects the live Bluesky account-creation method; locks every reviewed live-origin literal by file/origin/count so a new occurrence fails review; requires the full AppView and Flutter test recipes; and requires the controlled federated fixture's three `.test` origins plus unknown-host trap.
- First run: `GOTOOLCHAIN=go1.26.6 go test ./internal/app -run '^TestReleaseGatesUseFixturesInsteadOfLiveBluesky$' -count=1` passed on its first run. This is an honest first-run green: current test transports and release recipes were already fixture-only, but no automated inventory prevented future live-host or account-creation drift.
- Implement/refactor: Test-only coverage; no production fix was needed. Replaced a local integer formatter with `strconv.Itoa`, included future `.github/workflows/*.{yml,yaml}` automatically when present, and ran `gofmt`.
- Focused green: `GOTOOLCHAIN=go1.26.6 go test -race ./internal/app -run '^TestReleaseGatesUseFixturesInsteadOfLiveBluesky$' -count=1` passed.
- Offline AppView gate: The first `just test` run was intentionally concurrent with the full Flutter suite and analysis. It made no live-provider call but one synthetic short-timeout fixture asserted zero PAR observations instead of one under load. The isolated exact rerun, `TEST_DATABASE_URL=... TEST_DATABASE_REQUIRED=true GOTOOLCHAIN=go1.26.6 go test -race ./internal/app -run '^TestProviderRegistrationStartFailureClassificationDoesNotFallback$/timeout$' -count=1`, passed. Re-running `just test` alone then passed the full host-side AppView `go test -race ./...` suite against only local PostgreSQL/MinIO and controlled network fixtures.
- Offline Flutter gates: `just app-test` passed all 1,612 tests. `just app-analyze` passed with no issues, and Dart MCP analysis also reported no errors. Flutter registration tests use recording API clients and launchers and contain no live Bluesky origin or account-creation call.
- Diff/static evidence: Final `git diff --check` passed; the new Go test is formatted. No production, migration, Flutter, generated, release-smoke, or provider configuration file changed in Step 16.
- Notes: REG-010 is green for FR-016 and the automated half of AC-028. No live Bluesky request, account creation, disposable account, provider UI, or MAN-001 step was attempted. Final verification also ran the complete `just appview-check` release script. Its first ordinary-parallel run exposed a race-only test timing assumption, which was made deterministic by returning `context.DeadlineExceeded` from the PAR transport instead of relying on a 20 ms wall-clock deadline. A subsequent run exhausted the local PostgreSQL shared lock table while unrelated migration fixtures ran concurrently; both focused tests passed, and `GOFLAGS='-p=1' just appview-check` completed all release gates successfully.

### Release: MAN-001
- Manual evidence: Not run during coding.
- Required artifact: `release-smoke-evidence.md`.
- Notes: This remains a release blocker and requires a production-like signed build plus disposable live Bluesky account.

## Implementation Review Corrections

### IR-001: Registration Token Redaction And Issuance Certainty
- Linked contract: FR-011, FR-015, NFR-001, NFR-002, NFR-004; AC-011, AC-012, AC-014, AC-015, AC-026; UT-007, UT-011, IT-008, IT-017, IT-020.
- Write focused failing test: Added a real TLS token endpoint returning a provider-controlled 503 sentinel body and asserted `providerUnavailable`, durable `exchange_ambiguous`, and absence of the sentinel from global logs.
- Confirmed red: The callback returned the wrong trusted category, persisted `exchange_failed`, and Indigo logged the provider sentinel body.
- Implement: Routed only registration initial-token exchange through a scoped transport in `RegistrationOAuthAdapter`. The transport consumes failed response bodies before Indigo can log them, preserves PKCE/client assertions/DPoP and one nonce replay, and reports typed transience and issuance certainty.
- Extended coverage: Added transport loss after transmission, deadline, HTTP 429, definite HTTP 400 rejection, malformed HTTP 200, and DPoP nonce replay cases. Uncertain outcomes persist `exchange_ambiguous`; definite protocol rejection persists `exchange_failed`; transient upstream outcomes return `providerUnavailable`.
- Focused green: `go test -race ./internal/auth ./internal/app ./internal/federatedhttp -count=1` passed.
- Static evidence: `git diff --check` passed.

### IR-002: Production Stale-Exchange Reconciliation
- Linked contract: FR-004, FR-005, FR-011, NFR-001; AC-011, AC-016; IT-008, IT-016, REG-006.
- Write focused failing test: Added a real-PostgreSQL production-worker test with stale registration exchanges both with and without quarantined credentials. The test requires reconciliation before ordinary sweeping, retained terminal evidence, and correct shared-capacity accounting.
- Confirmed red: The ordinary sweep observed both fixtures still in `exchange_started`, proving the production worker never invoked reconciliation.
- Implement: Extended the production sweeper boundary with `ReconcileStaleRegistrationExchanges` and invoke it before each ordinary sweep. A reconciliation error is bounded and observed but does not prevent login/deletion sweeping; context cancellation still stops the worker.
- Focused green: `go test -race ./cmd/appview -run '^TestRunAuthRequestSweeper' -count=1` passed. The full `./cmd/appview` package and nearby registration admission/restart race tests also passed.
- Static evidence: `git diff --check` passed.

### IR-003: Registration Migration Authority Shapes
- Linked contract: FR-004, FR-009, RULE-001, RULE-004; AC-005, AC-027; IT-005, IT-021.
- Write focused failing test: Expanded the migration up/down/up table to reject a fully bound `ready` registration and NULL/blank provider origin or issuer, while retaining partial-authority rejection and valid ownerless/bound transition shapes.
- Confirmed red: Bound-ready, NULL-provider, and NULL-issuer inserts succeeded before the migration change. Blank metadata and partial authority already failed, validating the harness against existing constraints.
- Implement: Registration metadata now explicitly requires non-NULL provider origin and issuer. The authority constraint permits registration's null triple, but permits a complete positive triple only after `ready`; login and deletion retain mandatory complete positive authority.
- Focused green: `go test -race ./internal/db ./internal/auth -count=1` passed, including migration up/down/up, registration persistence, atomic binding, reconciliation, and cleanup coverage.
- Static evidence: `git diff --check` passed.

### IR-004: Flutter Registration Start Outcomes
- Linked contract: FR-011, FR-015; AC-014, AC-026; AT-006, UT-006, UT-007, UT-008, UT-011.
- Write focused failing test: Added a real HTTP 502 `registration_incomplete` envelope through Dio's error interceptor, `AuthApiClient`, the real `AuthController`, and Welcome's visible messenger outcome.
- Confirmed red: The actual 502 was classified as `RegistrationFailure.providerUnavailable`, proving the prior 400-based unit test did not represent the wire contract. Add Account also initially displayed generic copy.
- Implement: `RegistrationFailure.fromStartError` now inspects allowlisted `ApiServerError.details.appViewError` values before generic server fallback. Welcome and Add Account present localized bounded registration outcomes; account-limit copy remains distinct; browser-launch failure maps to `registrationIncomplete`.
- Extended coverage: Both 502 registration codes are retained by the interceptor and mapped distinctly; widget tests cover visible Welcome/Add Account outcomes, browser-launch failure, and account-limit copy.
- Focused green: `flutter test test/shared/api/providers/error_mapping_interceptor_test.dart test/auth/models/auth_error_test.dart test/auth/welcome_page_test.dart test/auth/sign_in_page_test.dart` passed all 26 tests. Dart MCP analysis reported no errors.
- Static evidence: Dart formatting required no changes and `git diff --check` passed.

### IR-001 Follow-Up: Recoverable Malformed-Success Credentials
- Linked contract: FR-011, NFR-001; AC-011, AC-012; IT-008, IT-017; coding-plan token cleanup rules at lines 191-195 and 298.
- Write focused failing test: Added a real HTTP 200 token response containing complete access/refresh token strings plus a numeric `sub` that pinned Indigo rejects. The test requires no owner/session activation, no token/body sentinel in logs or ordinary auth-request persistence, and structured quarantine ownership for cleanup.
- Confirmed red: The callback ended `exchange_ambiguous` with no quarantine and no revocation despite the bounded response containing recoverable token strings.
- Implement: The registration-scoped token transport now buffers at most 64 KiB of successful response data, safely decodes only string access/refresh cleanup fields, restores the exact bounded body for Indigo, and returns those cleanup fields alongside an Indigo decode failure. The callback sends recoverable fields through the existing quarantine/cleanup path; quarantine failure retains the existing immediate-revocation fallback. Undecodable or token-free malformed bodies remain ambiguous. Raw bodies are never persisted or parsed tolerantly.
- Focused green: `go test -race ./internal/app -run '^TestProviderRegistrationMalformedSuccessQuarantinesRecoverableTokens$' -count=1` passed. `go test -race ./internal/auth ./internal/app ./internal/federatedhttp -count=1` also passed.
- Release green: `GOFLAGS='-p=1' just appview-check` passed all release gates. Binary audit retains existing no-fix `GO-2026-5932`; source-symbol scanning reports no called vulnerable symbol.
- Static evidence: `gofmt` and `git diff --check` passed.

## Initial Final Verification
- `GOFLAGS='-p=1' just appview-check`: Passed all AppView release gates. The vulnerability scan reports the repository's existing `GO-2026-5932` advisory for the unmaintained `golang.org/x/crypto/openpgp` package, which has no fixed version.
- `just app-test`: Passed all 1,612 Flutter tests.
- `flutter clean && flutter pub get && flutter analyze`: Passed with no issues after clearing generated macOS ephemeral artifacts left by the concurrent test run.
- `git diff --check`: Passed.
- Final code-quality correction: Removed an unused test assignment found by `staticcheck` and replaced a race-sensitive PAR timeout assertion with deterministic timeout injection.
- Manual gap: `MAN-001` cannot be executed in this workspace without a production-like signed build and disposable live Bluesky account. No evidence file was fabricated.

## Implementation Review Correction Verification
- `GOFLAGS='-p=1' just appview-check`: Passed all AppView release gates after the IR-001 through IR-003 corrections. The first correction run correctly failed REG-010 because new migration cases repeated five reviewed live Bluesky provider/issuer literals; those new fixtures were changed to `.test` origins, the focused migration and release-inventory tests passed, and the full gate then passed. Binary audit still reports existing no-fix `GO-2026-5932`; source-symbol scanning reports no called vulnerability.
- `just app-test`: Passed all 1,616 Flutter tests after IR-004.
- `just app-analyze`: Passed with no issues.
- Focused correction race suites: `./internal/auth`, `./internal/app`, `./internal/federatedhttp`, `./internal/db`, and `./cmd/appview` passed.
- `git diff --check`: Passed during every correction loop and final verification.
- Manual gap: IR-005 / MAN-001 remains blocked by the required production-like signed build and disposable live Bluesky account. No `release-smoke-evidence.md` was fabricated.

## Live Registration-Start Correction
- Runtime diagnosis: The isolated dev database had recorded an early, incrementally applied version of uncommitted migration 61. Registration columns existed, but `oauth_auth_request_reservations`, `oauth_unverified_credentials`, `cleanup_pending`, and the final constraints did not. With no OAuth request rows present, migration 61 was replayed down/up; the credential-revocation and auth-request-sweeper errors stopped.
- Confirmed provider boundary: `https://bsky.social/.well-known/oauth-protected-resource` returns HTTP 404, while `https://bsky.social/.well-known/oauth-authorization-server` identifies the same origin as issuer and advertises `prompt=create`. The prior implementation treated the configured origin only as a protected resource, causing `registration_incomplete` before PAR.
- Write focused failing test: Added a real PostgreSQL/TLS federated-flow case whose configured provider is an authorization-server issuer and whose protected-resource endpoint returns 404. Before the correction, `StartRegistration` failed during discovery.
- Implement: On a definitive protected-resource HTTP 404 only, registration discovery now interprets the server-owned configured origin as the issuer and validates its authorization-server metadata against that exact origin. Transport, timeout, redirect, 429, 5xx, and malformed successful discovery responses retain their existing fail-closed behavior.
- Focused green: The new issuer-start test, existing protected-resource start test, start-failure classification suite, prompted PAR adapter suite, and `go test -race ./internal/auth ./internal/app -count=1` passed against real PostgreSQL.
- Live green: After rebuilding AppView, `POST /v1/auth/registrations` returned HTTP 200 with a `https://bsky.social/oauth/authorize` URL. Live PAR completed after Indigo's expected DPoP nonce replay. No credential-revocation or auth-request-sweeper errors recurred.
- Static evidence: `gofmt`, Dart MCP analysis, and `git diff --check` passed.

## Completion Checklist
- [x] All Must requirements covered by tests or documented gaps
- [ ] All planned Must tests passing (`MAN-001` remains the documented live-provider release blocker; every automated test passes)
- [x] Relevant regression tests passing
- [x] No unlinked behavior implemented
- [x] Docs updated
- [x] Manual release gate recorded or explicitly blocked
- [x] Implementation review completed; required automated findings corrected and ready for correction re-review
